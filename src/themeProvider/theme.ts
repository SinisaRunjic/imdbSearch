import { createTheme } from '@material-ui/core/styles'

const appTheme = createTheme({
  spacing: 5,
  palette: {
    primary: {
      light: '#fa4952',
      main: '#e52a30',
      dark: '#990008'
    },
  }
});
export default appTheme;
