import { createTheme } from '@material-ui/core/styles'

const appTheme = createTheme({
  spacing: 5,
  palette: {
    primary: {
      light: '#fff263',
      main: '#fbc02d',
      dark: '#c49000'
    },
  }
});
export default appTheme;
